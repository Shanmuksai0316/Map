/**
 * MAP-HMS Icon System
 * Export all icon-related components and utilities
 */

export {
  Icon,
  PrimaryIcon,
  AccentIcon,
  SuccessIcon,
  WarningIcon,
  ErrorIcon,
  MutedIcon,
  IconCatalog,
  IconSizes,
  type IconProps,
  type IconName,
  type IconSize,
} from '../Icon';

/**
 * Quick reference for common icon combinations used in MAP-HMS:
 * 
 * NAVIGATION:
 *   - Tab bars: use 'Outline' variants for inactive, filled for active
 *   - Example: homeOutline (inactive) → home (active)
 * 
 * STATUS BADGES:
 *   - approved: success color
 *   - pending: warning color  
 *   - rejected: error color
 *   - active: info color
 * 
 * ACTION BUTTONS:
 *   - Primary actions: filled icons, primary color
 *   - Secondary actions: outline icons, primary color
 * 
 * REQUESTS:
 *   - outpass: exit icon
 *   - leave: airplane icon
 *   - sickLeave: medkit icon
 *   - guestEntry: people icon
 *   - roomChange: swap-horizontal icon
 *   - complaint: chatbox-ellipses icon
 * 
 * STAFF ROLES:
 *   - guard: shield icon
 *   - warden: key icon
 *   - rector: ribbon icon
 *   - manager: business icon
 * 
 * SERVICES:
 *   - laundry: shirt icon
 *   - sports: football icon
 *   - maintenance: construct icon
 *   - housekeeping: sparkles icon
 */

