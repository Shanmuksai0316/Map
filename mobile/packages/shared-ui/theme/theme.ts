/**
 * Re-export theme from shared-core package
 * This allows shared-ui components to import theme consistently
 */
export { theme } from '../../shared-core/theme/theme';
export type { Theme, ThemeColors, ThemeSpacing } from '../../shared-core/theme/theme';

