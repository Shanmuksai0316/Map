appId: com.mapmars.hmsstaff
name: "RM Supervisor - E2E Test"
tags:
  - rm_supervisor
  - e2e
  - role-specific

---

# ============================================
# RM Supervisor Role E2E Test
# Phone: 9900000008 | OTP: 123456
# ============================================

- launchApp:
    appId: com.mapmars.hmsstaff
    clearState: true
    clearKeychain: true

- extendedWaitUntil:
    visible: "(Staff App|Phone Number|Login)"
    timeout: 30000

- takeScreenshot: "rm_supervisor/01_login"

# Enter phone number
- tapOn:
    text: "(Phone Number|Enter your registered)"
    optional: true
- inputText: "8888888886"
- hideKeyboard

- takeScreenshot: "rm_supervisor/02_phone_entered"

# Tap (Send OTP|Get OTP)
- tapOn: "(Send OTP|Get OTP)"

# Handle "Test Mode" dialog
- runFlow:
    when:
      visible: "Test Mode"
    commands:
      - tapOn: "OK"

# Wait for OTP screen
- extendedWaitUntil:
    visible: "(Verification Code|Enter OTP|6-digit)"
    timeout: 15000

- takeScreenshot: "rm_supervisor/03_otp_screen"

# Enter OTP
- inputText: "123456"
- hideKeyboard

# Tap Verify
- tapOn:
    text: "(Verify & Login|Verify)"
    optional: true

# Handle "Select Institution" screen
- runFlow:
    when:
      visible: "(Select Institution|Select Tenant)"
    commands:
      - takeScreenshot: "rm_supervisor/04_tenant_selection"
      - tapOn:
          text: "(Demo College|Demo|College)"
          optional: true

# Wait for Dashboard
- extendedWaitUntil:
    visible: "(RM Supervisor|Room Maintenance|Good Morning|Good Afternoon|Good Evening|Dashboard)"
    timeout: 30000

- takeScreenshot: "rm_supervisor/05_dashboard"

# Verify RM Supervisor-specific features
- assertVisible: "(RM Supervisor|Room Maintenance|Repairs|Maintenance)"
- takeScreenshot: "rm_supervisor/06_dashboard_verified"

# Navigate to Profile
- runFlow:
    when:
      visible: "Profile"
    commands:
      - tapOn: "Profile"
      - extendedWaitUntil:
          visible: "(Profile|Logout|RM Supervisor)"
          timeout: 10000
      - takeScreenshot: "rm_supervisor/07_profile"

- takeScreenshot: "rm_supervisor/08_test_complete"

